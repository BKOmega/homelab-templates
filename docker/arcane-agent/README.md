# Arcane agent

Remote Arcane agent template. Create the external `management` network (or change
`MANAGEMENT_NETWORK`), copy `.env.example` to `.env`, and provide the manager URL and the
agent token generated by your Arcane manager.

The agent mounts the Docker socket and can control the host. Keep its published port bound
to loopback or a trusted management address.
