# Uptime Kuma

Uptime Kuma with local application data. The production database, monitor inventory,
notifications, private CA certificate, and incident history are excluded. Add a private CA
at deployment time only if your monitored services require one.
