# TrueCommand

TrueCommand with persistent local application data. No TrueNAS systems, API credentials,
certificates, or production state are included. Keep this management interface on a
trusted network and add systems through the application after deployment.
